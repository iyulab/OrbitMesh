# OrbitMesh Environment Configuration
# Copy this file to .env and set your values
#
# cp .env.example .env
# nano .env  # Edit values
# docker compose up -d

# ===========================================
# REQUIRED VARIABLES
# ===========================================

# Admin password for the web console (REQUIRED)
# Use a strong password with at least 12 characters
ORBITMESH_ADMIN_PASSWORD=

# ===========================================
# OPTIONAL VARIABLES
# ===========================================

# Server configuration
# ASPNETCORE_ENVIRONMENT=Production

# Agent configuration (for additional agents)
# AGENT_NAME=agent-1
# AGENT_TAGS=docker,worker

# Enable shell execution on agents (security risk - use with caution)
# ENABLE_SHELL_EXECUTION=false
